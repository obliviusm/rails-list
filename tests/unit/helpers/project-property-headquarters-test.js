import { projectPropertyHeadquarters } from 'rails-list/helpers/project-property-headquarters';
import { module, test } from 'qunit';

module('Unit | Helper | project property headquarters');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = projectPropertyHeadquarters('San Francisco');
  assert.ok(result);
});
