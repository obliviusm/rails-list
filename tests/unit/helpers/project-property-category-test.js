import { projectPropertyCategory } from 'rails-list/helpers/project-property-category';
import { module, test } from 'qunit';

module('Unit | Helper | project property category');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = projectPropertyCategory([42]);
  assert.ok(result);
});
